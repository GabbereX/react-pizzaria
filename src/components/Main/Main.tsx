import React, { FC, Fragment } from 'react';
// import styles from './Main.module.scss';
import Shop from './Shop/Shop';

const Main: FC = () => {
  return (
    <Fragment>
      <Shop />
    </Fragment>
  );
};

export default Main;
